<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>관리자용 이벤트 수정페이지</title>
    <!-- css -->
    <link rel="stylesheet" href="/css/style.css">
    <!-- 웹폰트 및 아이콘 -->
    <%- include("common/icon_font.ejs") %>
</head>
<body>
    <!-- 상단 영역 -->
    <%- include("common/header.ejs") %>
    <!-- 중간 영역 -->
    <div id="container">
        <div class="event_update_area event_form_area">
            <div class="center">
                <form id="admin_event_form" class="event_area_form" action="/update/event" method="post" enctype="multipart/form-data">
                    <!-- 이벤트 번호를 알기 위한 히든태그 -->
                    <input type="hidden" name="event_no" value="<%- evtData.event_no %>">
                    <!-- 이벤트 이름 -->
                    <label for="title">이벤트 제목</label>
                    <input type="text" name="title" class="title" value="<%- evtData.event_title %>">
                    <!-- 이벤트 날짜 -->
                    <label for="date">이벤트 날짜</label>
                    <input type="text" name="date" class="date" value="<%- evtData.event_date %>">
                    <!-- 이벤트 파일 -->
                    <label for="file">이벤트 파일</label>
                    <input type="file" name="file" class="file">
                    <!-- 파일을 안 골랐을 때를 위한 히든태그 -->
                    <input type="hidden" name="origin_file" value="<%- evtData.event_file %>">
                    <!-- 이벤트 내용 -->
                    <label for="context">이벤트 내용</label>
                    <textarea name="context" class="context"><%- evtData.event_context %></textarea>
                
                    <button class="event_btn" type="submit">이벤트 수정</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>