//필요한 태그 생성
const card_box = document.querySelectorAll(".view .prd_list");
const prev_btn = document.querySelectorAll(".card_btn_set .prev_btn")
const next_btn = document.querySelectorAll(".card_btn_set .next_btn")